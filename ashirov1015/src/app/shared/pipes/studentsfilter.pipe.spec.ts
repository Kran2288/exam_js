import { StudentsfilterPipe } from './studentsfilter.pipe';

describe('StudentsfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentsfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
